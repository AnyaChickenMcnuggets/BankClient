<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.BankClient"
        tools:targetApi="31">
        <activity
            android:name=".activity.SelectIncomeExpenseActivity"
            android:exported="false" />
        <activity
            android:name=".activity.ExpenseActivity"
            android:exported="false" />
        <activity
            android:name=".activity.AddIEActivity"
            android:exported="false" />
        <activity
            android:name=".activity.IncomeActivity"
            android:exported="false"
            android:label="@string/title_activity_income"
            android:theme="@style/Theme.BankClient" />
        <activity
            android:name=".activity.AddPlanActivity"
            android:exported="false"
            android:parentActivityName=".activity.PlanListActivity" />
        <activity
            android:name=".activity.PlanListActivity"
            android:exported="false"
            android:label="@string/title_activity_plan_list"
            android:theme="@style/Theme.BankClient" />
        <activity
            android:name=".activity.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>